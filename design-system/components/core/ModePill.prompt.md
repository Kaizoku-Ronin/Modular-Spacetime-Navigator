The rounded mode indicator from the top nav — a dot plus the mode name, colored by mode (flight teal, starmap violet, jump white).

```jsx
<ModePill mode="flight" />
<ModePill mode="starmap" />
```

Modes: `flight` | `starmap` | `jump`. Sits on a glass background with a matching low-alpha border.
